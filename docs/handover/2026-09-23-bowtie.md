# Handover 2026-09-23 - general bowtie repair, live test in GO-260809v6

Moved from the Claude desktop app (cloud session) to Claude Code in VS Code. Read `CLAUDE.md` first for the standing rules.

## Goal of this thread
Make the corridor bowtie repair general (not Pasir Gudang specific): one call, `civil3d_corridor bowtie_fix`, that solves the
valley line of a bend, isolates the clash range (over one or two regions), swaps in a clip assembly, maps
ClipTarget + ClipElev, rebuilds, checks, and rolls back cleanly on failure. `bowtie_unfix` undoes a repair.

Decisions by Amir:
- Level move is spread from the hinge over every slope and bench (clip v0.4, "Spread From Hinge" = Yes), BUT the design
  ratio is the maximum: steeper than designed, or a bench whose fall reverses, is flagged for remedial protection.
- Valley level rule: `no_steeper` (default; cut side only lowered, fill side only raised) with `mean` as an option; where no
  such level exists the mean is used and flagged.
- Junctions are out of scope (separate topic, Amir's own workflow).

## State of the code (laptop repo, all committed, working tree clean; not pushed)
- `83f6133` (built in the cloud, NOT yet loaded in Civil 3D - Amir is rebuilding now):
  - bowtie_fix gives the repaired region(s) 1 m frequency (`frequency` arg); isolate records `parentFrequency`, the pieces
    record carries it, bowtie_unfix puts it back after the merge.
  - bowtie_unfix restores EVERY piece of a two-region repair (not only the one named), and deletes each meet station from
    the piece that holds it; result has `pieces`; the TS rollback skips pieces an earlier unfix already restored.
  - Repairs are named after the apex station and side: `BT-384L`, two regions `BT-384La` / `BT-384Lb` (was a counter: BT-577).
  - `bowtie_fix_report` (read-only): the last 20 bowtie_fix reports kept in the MCP server's memory. Needed because a remote
    client gave up after 60 s while the fix finished.
  - `region_frequency` takes `regionName` and refuses when no region is given (it silently fell back to region 0 and changed
    the wrong region).
- `a66f2f7` (tested live today, works): per-piece clip assemblies via `assemblyMap`, fixPieces record, seam trims its search
  window at a clipped neighbour bend, closure > 0.02 only a warning with ClipElev.
- `ff01a25` and earlier: kernel (spread, slope flags, NoSteeper, section changes, open-ended sections, bench candidates,
  overshoot guard, Unresolved status), 42 kernel tests, clip v0.4 (1440 offline cases).

## State of the drawing GO-260809v6.dwg (saved 12:08 by QSAVE)
- Assemblies added: `MD100 Drain 1.5 LDC04`, `MD302 Drain 3.3 LDC04`, `MD303 Drain 3.6 LDC04` (UTNM_MainDrainU + clip v0.4
  each side: left lane 3.7 on DrainTopOut_L, right lane 1.0 on DrainTopOut_R, lane slope 0, cut/fill 0.5, max heights 4,
  bench 1 m at 0.02, Spread From Hinge = Yes).
- Drain FL-03: region `BT-577` (341-349, MD100 LDC04) repaired, valley line "Drain FL-03 BT-577 Valley R".
  8 -> 0 crossings, level move 0.002 m, no slope flags. (The old v0.3 repair of BT-346R was unfixed first.)
- Drain FL-02: regions `BT-771a` (381-384.1696, MD302 LDC04) and `BT-771b` (384.1696-387, MD303 LDC04), both mapped to
  "Drain FL-02 BT-771b Valley L", frequency set to 1 m by hand. bowtie_check 375-393 clean on both sides.
  Region 0 of FL-02 was wrongly set to 1 m by the region_frequency bug and set back to 5 m.
- Design flags on FL-02 reported to Amir (not decided yet):
  1. Left cut slope at 383.43 is 1:1.76 (hinge -5.65/+3.30 to valley -8.67/+5.02), steeper than the designed 1:2.
  2. MD302 -> MD303 at 384.17: drain top 0.3 m higher on MD303; the valley takes the mean, so at 384.0 the left lane rises
     0.15 m within 4 cm of the drain wall. Belongs to the junction topic.
- Other bends still on the old v0.3 LDC assemblies (BT-141L, BT-01, BT-310R, BT-404L, BT-576R on FL-03; BT-179L, BT-291R,
  BT-638R, BT-770L on FL-02) - not touched.

## Round trip done 12:37 (83f6133 live)
- bowtie_unfix of BT-771b restored BOTH pieces (MD302 Drain 3.3 / MD303 Drain 3.6, parent names back, both meet stations
  removed). The MD303 region came back at 1 m (repair predated parentFrequency) and was set to 5 m with region_frequency
  + regionName (hit index 5, correct).
- bowtie_fix again: regions BT-384La (381-384.1696, MD302 LDC04) + BT-384Lb (384.1696-387, MD303 LDC04), both 1 m,
  parentFrequency 5/5/5/5 recorded; valley line "Drain FL-02 BT-384Lb Valley L"; 4 -> 0 crossings; bowtie_check 375-393 clean
  both sides. Highlight: section change 384.17-385 (inside links 1.65 -> 1.8 m, hinge 5.65 -> 5.8 m) and steeper than
  design at 383.43 (1:2 designed, 1:1.76 built). Level move 0.21 m. QSAVE done.
- The call timed out at 60 s (took 64 s); bowtie_fix_report returned the full report. It asked for approval (the word
  "fix" in the name) - fixed in approvalPolicy.ts after this run (read-only *_report actions need none).

## Whole-corridor run done 13:20 (c700d6c plugin, 2abdc54 server) - the general workflow works
One `bowtie_fix {name, assemblyMap, redo: true, background: true}` per corridor, followed with `bowtie_fix_report`.
- FL-03 (MD100 -> MD100 Drain 1.5 LDC04), 10 min: 8 bends, 7 repaired (BT-141L, BT-207R, BT-310R, BT-404L, BT-576R replaced
  from v0.3; BT-274R and BT-485L never repaired before), 1 skipped (BT-577, already v0.4), 0 failed.
- FL-02 (MD302 / MD303 -> LDC04), 7 min: 6 bends, 5 repaired (BT-179L, BT-291R, BT-638R, BT-770L replaced; BT-102R new),
  1 skipped (BT-384La/b, already v0.4), 0 failed.
- Every old repair recorded nothing about its parent; bowtie_unfix inferred it from the neighbours (same assembly both sides)
  and merged back, so no stray regions were left.
- bowtie_check over both whole corridors: clean (FL-03 195 stations, FL-02 233 stations; 0 link crossings, no loops). QSAVE.
- Slope flags for the engineer (steeper than the designed 1:2; remedial / extra protection):
  | Corridor | Station | Side | Built | Strip (offset from CL) |
  |---|---|---|---|---|
  | FL-02 | 383.43 | left | 1:1.76 | 5.65-8.67 m (plus the section change 384.17-385, junction topic) |
  | FL-03 | 404.32 | left | 1:1.78 | 4.75-5.47 m (checked in the built section: 0.41 rise over 0.72 run) |
  | FL-03 | 274.74 | right | 1:1.99 | 2.05-10.05 and 11.05-13.09 m |
  | FL-02 | 102.26 | right | 1:1.99 | 2.95-6.85 m |
- New in the tools: bowtie_fix `redo` (undo + fix, previous repair made again if the new one fails -> kept_previous),
  `background` + progress in bowtie_fix_report, one fix at a time; bowtie_unfix `inferParent` (default on); bowtie_bends
  lists each region's assembly; tool inputs accept "true"/"2.5" text for booleans/numbers (stale client schemas).
- Skill corridor-bowtie-fix rewritten around this workflow (proposed to Amir 13:25).

## bowtie_refresh live test done 16:05 (plugin e2871e8 on the laptop = cloud 39da15c)
Drain FL-03, 8 repairs, all on MD100 Drain 1.5 LDC04. Every run about 35 s, `background: true` + `bowtie_fix_report`.
1. No change, dry run: 8 unchanged, `restore.restored: true`, all 8 still mapped.
2. Stock only (MD100 Drain 1.5, Daylight L/R `1309` 0.5 -> 0.45 = 1:2.22), dry run: 7 `refused_stale_assembly`, each with the
   link and grades (50 % clip against 45 % stock; BT-274R also a width 8 m against 8.889 m). BT-577 came out "unchanged":
   its record names the v0.3 assembly "MD100 Drain 1.5 LDC" as parent, which is on neither neighbour, so the drift check
   compared nothing and said nothing. Fixed in 542e2dc (below).
3. The same change in the clip (MD100 LDC04 L/R `Cut Slope` 0.45): dry run 8 would_refresh, restore ok; real run 8 refreshed,
   whole-corridor bowtie_check clean. Moves 0.02-1.7 m plan, up to 0.53 m level. Flags read against the new ratio
   (BT-404L 1:1.78 against 1:2.22; BT-274R 1:2.2 against 1:2.22).
   Report problems (fixed in 542e2dc): BT-577 "moved 31 m" = the level run-on past the valley end growing 1.1 m -> 30 m
   (OvershootMax), not the valley; BT-310R's new incoming meet station 309.006 was not added (1 m station 309.0 beside it), so
   the record held 1 station against 2 solved and it could never count as unchanged.
4. Both back to 0.5, real refresh: 8 refreshed; the four valleys saved before the test (BT-141L, BT-274R, BT-404L, BT-577)
   came back identical to 0.000 mm in plan and level, every meet station back to its value, flags back (1:1.99 at
   274.74, 1:1.78 at 404.32). bowtie_check clean (195 stations), all 8 mapped. QSAVE 16:03.

542e2dc (laptop; cloud be8ade3), built, NOT yet loaded:
- `ReconcileParentAssembly`: the neighbours that are neither pieces of the repair nor repairs decide the parent assembly; a
  recorded one on neither is stale and replaced (with a note). Used by the refresh drift check (every neighbour not compared
  is now reported, and "no region next to it on the parent assembly: not checked") and by bowtie_unfix
  (`parentAssemblyCorrected` in the plan). A refreshed single-region repair writes the corrected parent into its record.
- Refresh compares valleys without the run-on (`Refresh.WithoutRunOn`: the recorded `runOn` length, or the last segment when
  it is level for older lines - same rule on both lines) and meet stations as solved (`Refresh.StationShift`; record `meet=`).
  bowtie_seam and the refresh now record `meet=` and `runOn=`. Dry run reports the new meet stations (`meetStations.after`,
  `added` for the ones that went in).
- 47 kernel tests.
Checked live 16:40 with 542e2dc loaded:
- No-change dry run: 8 unchanged (plan 0, level 0, meet stations 0 - BT-310R included), restore ok; BT-577 compared at both
  boundaries ("same design") with the note "recorded parent assembly 'MD100 Drain 1.5 LDC' is on neither region next to it
  (a stale record); taken from ...: MD100 Drain 1.5".
- Stock only 0.45, dry run: 8 of 8 `refused_stale_assembly`, BT-577 included; restore ok. Stock put back to 0.5.
- `bowtie_unfix` dryRun BT-577: `restoreAssembly` MD100 Drain 1.5, `parentAssemblyCorrected` listed - but `mergeWith` [] and
  `restoreName` "BT-346R": the record names the v0.3 region it was cut whole out of and nothing either side, so a real unfix
  would have left a stray 1 m region renamed BT-346R. Fixed in e25db11 (cloud; laptop commit below, NOT yet loaded): a
  record with no regions to merge with is merged with the regions either side when they share one assembly and are not
  repairs, with their first one's name (and frequency when none was recorded), and says so.
- The dry run leaves the corridor built with the design as it was during the run; an assembly edit afterwards does not
  rebuild it (right side showed 689 links against 688 until a rebuild). Rebuilt, bowtie_check clean (195 stations, 688 right
  links as saved), QSAVE 16:46.
Checked live 18:58 with 12c4744 loaded: `bowtie_unfix` dryRun BT-577 -> restoreAssembly MD100 Drain 1.5, mergeWith both
neighbours ('RG - MD100 Drain 1.5 (2) (2) (2) (2) (3)' and 'RG - MD100 Drain 1.5 (2) (2) (2)'), restoreName the first one,
restoreFrequency 5/5/5/5, with the warning saying why. Nothing changed in the drawing (dry run).
All bowtie_refresh / bowtie_unfix work of today is live-tested; laptop commits 542e2dc + 12c4744 (+ this note) ready to push.

## Next steps
1. (done - see above) After Amir's rebuild + NETLOAD: `civil3d_health`, then round trip on FL-02 384:
   - `bowtie_unfix` dryRun on `BT-771b` - the plan must list both pieces (BT-771a -> MD302 Drain 3.3, BT-771b -> MD303 Drain 3.6).
   - `bowtie_unfix` for real. The FL-02 pieces were made before `parentFrequency` was recorded: check the merged MD302 and
     MD303 regions come back at 5 m; fix by hand with `region_frequency` + `regionName` if not.
   - `bowtie_fix` name "Drain FL-02", startStation 383.1696, endStation 385.1696,
     assemblyMap {"MD302 Drain 3.3": "MD302 Drain 3.3 LDC04", "MD303 Drain 3.6": "MD303 Drain 3.6 LDC04"}.
     Expect regions BT-384La/b at 1 m, highlight with the section change and the 1:1.76 slope flag.
     If the call times out, `bowtie_fix_report` with name "Drain FL-02".
   - `bowtie_check` 375-393, then QSAVE.
2. (done - see above) bowtie_refresh for feature-line valleys. Still open: bowtie_check honest status and continuity sampling.
3. Housekeeping: corridor create rebuilds before targets are mapped; feature_line_list null names.
4. UTNMBenchClip generic clip twin with the spread, once v0.4 is proven on more bends.
5. Rewrite the corridor-bowtie-fix skill around the one-call bowtie_fix (predict -> bends -> fix -> check -> report flags).
6. Update the project doc `bowtie-fix-plan` (claude.ai project "Civil 3D MCP") with today's results.

## Useful facts
- Snapshot of FL-03 346 for offline solving: `D:\Claude\Civil 3D MCP\Claude outputs\scratch\fl03-346-snapshot.json`
  (`dotnet run --project bowtie-kernel/BowtieKernel.Tests -- solve <snap> --spread`).
- bowtie_seam dryRun is the read-only preview of a fix (pass `clipAssembly` so adjustFrom auto reads the clip part);
  bowtie_fix itself always needs approval.
- The seam refuses sections that are already clipped (Valley point) inside its range: unfix first.
- create_subassembly attaches with `parameters.attachToSubassembly` + `parameters.attachToPointCode`.
